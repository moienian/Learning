import React from "react";
import ReactDOM from "react-dom";
import { createStore } from "redux";

const root = document.getElementById("root");
const el = React.createElement("p", null, "test");
ReactDOM.render(el, root);
